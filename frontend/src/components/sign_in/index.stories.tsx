import { Meta, StoryObj } from '@storybook/react'
import SignInContainer from '.'

export default {
  title: 'Components/Sign In',
  component: SignInContainer
} as Meta

type Story = StoryObj<typeof SignInContainer>

export const Primary: Story = {}
